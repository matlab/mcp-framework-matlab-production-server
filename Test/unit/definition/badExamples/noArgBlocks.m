function [x,y] = noArgBlocks(a,b,c)
% This function has a beautiful, poetic description, but no argument
% blocks. So mcpDescription will error when processing this file.
    x = a + b + c;
    y = a - b - c;
end
